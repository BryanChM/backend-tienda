package com.guepardosport.backend_tienda.repository;

import com.guepardosport.backend_tienda.entity.PrendaColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrendaColorRepository extends JpaRepository<PrendaColor, Long> {
}